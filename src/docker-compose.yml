version: '3.4'

services:
  eshopdb:
    image: postgres
  
  distrubutedcache:
    image: redis
    
  seq:
    image: datalust/seq:latest
  
  messagebus:
    image: rabbitmq:management

volumes:
  postgres_eshopdb: